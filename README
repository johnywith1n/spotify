Open the file in a browser to use it.

The app is written using jQuery and Bootstrap. I wasn't sure what was meant by "page", but each section is not a separate page. This is a single page app. When clicking on an artist, the top tracks will show up in a modal because I did not want to lose the current search results.